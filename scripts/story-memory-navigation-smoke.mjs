import assert from'node:assert/strict';import fs from'node:fs';const idx=fs.readFileSync('index.html','utf8'),mem=fs.readFileSync('js/story-memory-v2.js','utf8'),nav=fs.readFileSync('js/world-exit-navigation.js','utf8'),v=JSON.parse(fs.readFileSync('version.json','utf8')).version;assert.equal(v,'20260821-2');for(const f of ['story-memory-v2.js','world-exit-navigation.js'])assert.ok(idx.includes(`js/${f}?v=${v}`),`${f} not loaded`);for(const x of ['storyMemoryVersion','storyMemoryMode','voiceSummary','examples','分析既有主線收藏','只學語氣與習慣，不記事件'])assert.ok(mem.includes(x),`story memory missing ${x}`);for(const x of ['ocWorldExitBtn','返回我的世界','OCLifeWorldContext?.clear','backBtn'])assert.ok(nav.includes(x),`navigation missing ${x}`);console.log('Story memory and world exit validation passed.');