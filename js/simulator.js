(function(){
'use strict';
const S=()=>window.OCLifeStore,AI=()=>window.OCLifeAI;const pick=a=>a[Math.floor(Math.random()*a.length)];
const T={activities:['在整理手邊的東西','剛忙完一件事，正在放空','坐著發呆','在吃東西','在看訊息','準備出門','剛回到住處','在處理工作','在做自己的興趣','什麼也沒做，只是休息'],moods:['平靜','放鬆','有點累','心情不錯','專注','有點無聊'],places:['住處','工作地點','街上','咖啡店','房間','附近']};
function chars(worldId){return S().all('characters',{worldId})}function statusOf(id){return S().all('statuses').find(s=>s.characterId===id)}
function tickStatuses(worldId){for(const c of chars(worldId)){const st=statusOf(c.id);if(!st||Math.random()<.58)continue;S().update('statuses',st.id,{place:pick(T.places),activity:pick(T.activities),mood:pick(T.moods),updatedAt:Date.now()})}}
function rel(a,b){return(a.relationshipLinks||[]).find(x=>x.characterId===b.id)||null}function cfg(){const s=S().data.settings||{};return{provider:s.provider||'none',baseUrl:s.baseUrl||'',apiKey:s.apiKey||'',model:s.model||''}}
function parseJSON(text){let t=String(text||'').trim().replace(/^```(?:json)?\s*/i,'').replace(/\s*```$/,'');try{return JSON.parse(t)}catch(_){const m=t.match(/\{[\s\S]*\}/);if(m)return JSON.parse(m[0]);throw new Error('AI 格式無法解析')}}
function charProfile(c){const ip=c.interactionProfile||{};return`姓名：${c.name}\n身份：${c.identity||'未設定'}\n性格：${c.personality||'未設定'}\n性格底層：${ip.core||''}\n外在表現：${ip.outer||''}\n親密關係中的樣子：${ip.intimate||''}\n情緒反應：${ip.emotionalReactions||''}\n主動性：${ip.initiative||''}\n親密表達：${ip.affection||''}\n說話方式：${c.speech||ip.speechRhythm||''}\n習慣：${c.habits||ip.physicalHabits||''}\n背景：${c.background||''}`}
function memoryText(chat){return(Array.isArray(chat.memories)?chat.memories:[]).slice(-12).map((m,i)=>`${i+1}. ${typeof m==='string'?m:m.text||''}`).filter(Boolean).join('\n')}
function saveMemory(chat,text){text=String(text||'').trim();if(!text)return;const old=Array.isArray(chat.memories)?chat.memories:[];if(old.some(m=>String(typeof m==='string'?m:m.text||'').trim()===text))return;S().update('chats',chat.id,{memories:[...old,{id:S().uid('memory'),text,createdAt:Date.now(),source:'ai-chat'}].slice(-40)})}
function stripActions(text){let t=String(text||'').trim();t=t.replace(/\*[^*\n]{1,120}\*/g,'').replace(/（[^）\n]*(?:笑|看|摸|抱|親|吻|走|坐|站|靠|伸|抬|低|轉|皺|挑眉|嘆|動作|神情)[^）\n]*）/g,'').replace(/\([^\)\n]*(?:smile|look|touch|hug|kiss|walk|sit|stand|sigh)[^\)\n]*\)/gi,'');return t.replace(/\s{2,}/g,' ').trim()}
async function aiChat(worldId,a,b,chat){const c=cfg();if(c.provider==='none'||!c.apiKey||!c.model)throw new Error('AI 未設定');const w=S().get('worlds',worldId),ra=rel(a,b),rb=rel(b,a),sa=statusOf(a.id),sb=statusOf(b.id),recent=S().all('messages',{chatId:chat.id}).sort((x,y)=>x.at-y.at).slice(-14).map(m=>`${S().get('characters',m.senderId)?.name||'角色'}：${m.text}`).join('\n'),memories=memoryText(chat);const prompt=`模擬兩名OC在聊天軟體裡自然聊天。\n【世界】${w?.name||''}\n${w?.summary||''}\n硬規則：${w?.rules||''}\n【A】${charProfile(a)}\n現在：${sa?.place||''}｜${sa?.activity||''}｜${sa?.mood||''}\n【B】${charProfile(b)}\n現在：${sb?.place||''}｜${sb?.activity||''}｜${sb?.mood||''}\n【關係】${ra?.type||rb?.type||'未標明'}\nA看B：${ra?.perspective||''}\nB看A：${rb?.perspective||''}\n共同：${ra?.sharedFacts||rb?.sharedFacts||''}\n熟悉程度：${ra?.intimacyLevel||rb?.intimacyLevel||''}\n相處模式：${ra?.interactionStyle||rb?.interactionStyle||''}\nA對B稱呼：${ra?.nicknames||'未設定'}\nB對A稱呼：${rb?.nicknames||'未設定'}\n長期記憶：${memories||'暫無'}\n最近聊天：${recent||'暫無'}\n生成4到8則短訊息。關係熟悉度和性格要直接體現在語氣、默契、稱呼、接話方式。情侶不可像陌生人寒暄。\n【硬性格式】這是純文字聊天介面，只能輸出角色實際打出的文字。禁止任何動作、神態、心理、場景旁白或舞台指示；禁止星號動作如 *笑*；禁止括號動作如（摸摸頭）（看著你）；不要用“他笑了”“她靠過來”等敘述。角色想表達情緒只能透過真正會發出去的文字、標點、語氣詞。\n只輸出JSON：{"messages":[{"sender":"姓名","text":"純聊天文字"}],"memory":"有長期價值的新記憶，沒有留空"}`;const out=await AI().call({...c,temperature:.88,maxTokens:2000,messages:[{role:'system',content:'高一致性OC聊天模擬器。輸出必須是純聊天訊息，絕不寫動作描寫、神態、心理或旁白。'},{role:'user',content:prompt}]});const d=parseJSON(out);return{messages:Array.isArray(d.messages)?d.messages:[],memory:String(d.memory||'').trim()}}
async function generateForChat(chat){if(!chat)throw new Error('找不到聊天室');const worldId=chat.worldId,a=S().get('characters',chat.memberIds[0]),b=S().get('characters',chat.memberIds[1]);if(!a||!b)throw new Error('聊天室角色不存在');const g=await aiChat(worldId,a,b,chat);if(!g.messages.length)throw new Error('空回覆');const base=Date.now()-g.messages.length*900,made=[];for(let i=0;i<g.messages.length;i++){const x=g.messages[i],sender=S().namesEquivalent(x.sender,a.name)?a:S().namesEquivalent(x.sender,b.name)?b:null,text=stripActions(x.text);if(sender&&text)made.push(S().add('messages',{chatId:chat.id,worldId,senderId:sender.id,text,at:base+i*900}))}if(!made.length)throw new Error('沒有有效訊息');if(g.memory)saveMemory(chat,g.memory);S().update('chats',chat.id,{lastAt:Date.now()});return made}
async function generateChatById(chatId){const chat=S().get('chats',chatId);return generateForChat(chat)}
async function autonomousChat(worldId){const list=chars(worldId);if(list.length<2)return null;let pairs=[];for(const a of list)for(const l of(a.relationshipLinks||[])){const b=S().get('characters',l.characterId);if(b&&b.worldId===worldId&&a.id<b.id)pairs.push([a,b])}let a,b;if(pairs.length&&Math.random()<.85)[a,b]=pick(pairs);else{a=pick(list);b=pick(list.filter(x=>x.id!==a.id))}return generateForChat(S().ensureChat(worldId,a.id,b.id))}
async function aiMoment(worldId,c){const conf=cfg();if(conf.provider==='none'||!conf.apiKey||!conf.model)throw new Error('AI 未設定');const w=S().get('worlds',worldId),st=statusOf(c.id),recent=S().all('moments',{worldId}).filter(m=>m.characterId===c.id).sort((a,b)=>b.at-a.at).slice(0,5).map(m=>m.text).join('\n---\n'),related=(c.relationshipLinks||[]).map(l=>{const o=S().get('characters',l.characterId);return o?`${o.name}：${l.type||'關係'}；${l.perspective||''}；稱呼${l.nicknames||''}`:''}).filter(Boolean).join('\n');const prompt=`替OC生成一條像真人社交平台/朋友圈的動態。世界：${w?.name||''} ${w?.summary||''}\n角色：${charProfile(c)}\n現在：${st?.place||''}｜${st?.activity||''}｜${st?.mood||''}\n重要關係：${related||'無'}\n最近動態：${recent||'暫無'}\n必須像角色本人發的文字，不要程序匯報。動態正文也禁止用星號或括號寫動作描寫；它是角色真正發出的貼文。comments也只能是留言文字。只輸出JSON：{"text":"動態正文","comments":[{"character":"留言角色姓名","text":"留言"}]}`;const out=await AI().call({...conf,temperature:1,maxTokens:1200,messages:[{role:'system',content:'OC社交動態模擬器。只寫角色實際發布的文字，不寫動作旁白。'},{role:'user',content:prompt}]});return parseJSON(out)}
async function generateMomentForCharacter(characterId){const c=S().get('characters',characterId);if(!c)throw new Error('角色不存在');const worldId=c.worldId,list=chars(worldId),g=await aiMoment(worldId,c),text=stripActions(g.text);if(!text)throw new Error('空動態');const moment=S().add('moments',{worldId,characterId:c.id,text,at:Date.now(),likes:[],comments:[]});for(const cm of(Array.isArray(g.comments)?g.comments:[])){const o=list.find(x=>S().namesEquivalent(x.name,cm.character)),ct=stripActions(cm.text);if(o&&o.id!==c.id&&ct)moment.comments.push({id:S().uid('comment'),characterId:o.id,text:ct,at:Date.now()+1000})}S().save();return moment}
function leastRecentlyPosted(worldId){const list=chars(worldId);if(!list.length)return null;const lastMap=new Map(list.map(c=>[c.id,0]));for(const m of S().all('moments',{worldId})){if(lastMap.has(m.characterId)&&m.at>lastMap.get(m.characterId))lastMap.set(m.characterId,m.at)}const sorted=list.map(c=>({c,last:lastMap.get(c.id)||0})).sort((a,b)=>a.last-b.last);const min=sorted[0].last;const pool=sorted.filter(x=>x.last===min).map(x=>x.c);return pick(pool)}
async function autonomousMoment(worldId){const c=leastRecentlyPosted(worldId);if(!c)return null;return generateMomentForCharacter(c.id)}
function simulate(worldId,opts={}){tickStatuses(worldId);if(opts.chat!==false&&Math.random()<.75)autonomousChat(worldId);if(opts.moment!==false&&Math.random()<.55)autonomousMoment(worldId)}
window.OCLifeSimulator={simulate,tickStatuses,autonomousChat,autonomousMoment,generateChatById,generateMomentForCharacter};
})();