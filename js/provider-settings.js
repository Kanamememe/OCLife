(function(){
'use strict';
const S=window.OCLifeStore,AI=window.OCLifeAI,$=s=>document.querySelector(s);
const PROFILE_KEY='oclife_api_profiles_v2';
const PRESETS={
  none:{label:'不使用 API',baseUrl:'',hint:'只使用不需要 AI 的本機功能'},
  siliconflow:{label:'硅基流動 SiliconFlow',baseUrl:'https://api.siliconflow.cn/v1',hint:'切換後會自動填入 SiliconFlow 地址'},
  deepseek:{label:'DeepSeek 官方 API',baseUrl:'https://api.deepseek.com',hint:'切換後會自動填入 DeepSeek 地址'},
  gemini:{label:'Google Gemini',baseUrl:'https://generativelanguage.googleapis.com/v1beta',hint:'切換後會自動填入 Gemini 地址'},
  openai:{label:'OpenAI 官方 API',baseUrl:'https://api.openai.com/v1',hint:'切換後會自動填入 OpenAI 地址'},
  custom:{label:'OpenAI-compatible／自訂',baseUrl:'',hint:'請自行填入相容服務的 Base URL'}
};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function current(){const s=S.data.settings||{};return{provider:s.provider||'none',baseUrl:s.baseUrl||'',apiKey:s.apiKey||'',model:s.model||''}}
function auto(){return window.OCLifeAutoLife?.settings?.()||{enabled:true,minMinutes:8,maxMinutes:22,chat:true,moment:true,event:true,status:true}}
function profiles(){try{const x=JSON.parse(localStorage.getItem(PROFILE_KEY)||'[]');return Array.isArray(x)?x:[]}catch(_){return[]}}
function saveProfiles(list){localStorage.setItem(PROFILE_KEY,JSON.stringify(list))}
function close(){document.getElementById('modalRoot').innerHTML=''}
function modal(h){document.getElementById('modalRoot').innerHTML=`<div class="modal-bg"><div class="modal" style="max-height:92dvh;overflow:auto">${h}</div></div>`}
function selected(){const list=$('#apiModelList'),manual=$('#apiModelManual');return{provider:$('#apiProvider')?.value||'none',baseUrl:($('#apiBase')?.value||'').trim(),apiKey:($('#apiKey')?.value||'').trim(),model:(manual?.value||list?.value||'').trim()}}
function validate(x){if(x.provider==='none')return'';if(!x.baseUrl)return'請先填入 Base URL';if(!x.apiKey)return'請先填入 API Key';if(!x.model)return'請先填入模型名稱';return''}
function setStatus(text,error=false){const el=$('#apiStatus');if(el){el.textContent=text||'';el.style.color=error?'#a55353':''}}
function persist({quiet=false}={}){const x=selected(),error=validate(x);if(error){setStatus(error,true);return false}S.data.settings={...S.data.settings,...x};S.save();setStatus(x.provider==='none'?'✓ 已保存：目前不使用 API':`✓ 已保存 ${PRESETS[x.provider]?.label||x.provider} · ${x.model}`);if(!quiet)window.OCLifeAutoLife?.toast?.('✓ API 設定已保存');return true}
function renderProfiles(){const box=$('#apiProfiles');if(!box)return;const list=profiles();box.innerHTML=list.length?list.map(p=>`<div style="display:flex;align-items:center;gap:8px;padding:9px 0;border-top:1px solid var(--line)"><div style="min-width:0;flex:1"><b style="font-size:12px">${esc(p.name)}</b><div class="note" style="white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${esc(PRESETS[p.provider]?.label||p.provider)} · ${esc(p.model||'未選模型')}</div></div><button type="button" class="secondary" data-api-load="${p.id}" style="padding:7px 9px">載入</button><button type="button" class="danger" data-api-delete="${p.id}" style="padding:7px 9px">刪除</button></div>`).join(''):'<div class="note">還沒有保存的 API 設定檔</div>';box.querySelectorAll('[data-api-load]').forEach(b=>b.onclick=()=>loadProfile(b.dataset.apiLoad));box.querySelectorAll('[data-api-delete]').forEach(b=>b.onclick=()=>{const p=profiles().find(x=>x.id===b.dataset.apiDelete);if(!p||!confirm(`刪除 API 設定檔「${p.name}」？`))return;saveProfiles(profiles().filter(x=>x.id!==p.id));renderProfiles()})}
function applyFields(x){if(!x)return;$('#apiProvider').value=x.provider||'custom';$('#apiBase').value=x.baseUrl||'';$('#apiKey').value=x.apiKey||'';$('#apiModelManual').value=x.model||'';$('#apiModelList').innerHTML='<option value="">尚未讀取模型</option>';$('#apiModelList').classList.add('hidden');updateProviderHint()}
function loadProfile(id){const p=profiles().find(x=>x.id===id);if(!p)return;applyFields(p);setStatus(`已載入「${p.name}」，按「保存目前 API」才會正式套用`)}
function saveProfile(){const x=selected(),error=validate(x);if(error){setStatus(error,true);return}const name=($('#apiProfileName').value||'').trim()||`${PRESETS[x.provider]?.label||x.provider} · ${x.model}`;const list=profiles(),same=list.find(p=>p.name===name),item={id:same?.id||`api_${Date.now()}_${Math.random().toString(36).slice(2,7)}`,name,...x,updatedAt:Date.now()};saveProfiles(same?list.map(p=>p.id===same.id?item:p):[item,...list]);$('#apiProfileName').value='';renderProfiles();setStatus(`✓ 已保存 API 設定檔「${name}」`)}
function updateProviderHint(){const id=$('#apiProvider')?.value||'none',p=PRESETS[id]||PRESETS.custom;const hint=$('#apiProviderHint');if(hint)hint.textContent=p.hint}
function changeProvider(){const id=$('#apiProvider').value,p=PRESETS[id]||PRESETS.custom;$('#apiBase').value=p.baseUrl;$('#apiModelManual').value='';$('#apiModelList').innerHTML='<option value="">尚未讀取模型</option>';$('#apiModelList').classList.add('hidden');updateProviderHint();setStatus(id==='custom'?'已切換為自訂服務，請填寫 Base URL':p.baseUrl?`已自動填入：${p.baseUrl}`:'已切換為不使用 API')}
async function loadModels(){const x=selected();if(x.provider==='none')return setStatus('請先選擇 API 服務商',true);if(!x.baseUrl)return setStatus('請先填入 Base URL',true);if(!x.apiKey)return setStatus('請先填入 API Key',true);const btn=$('#loadApiModels');btn.disabled=true;setStatus('正在讀取模型列表…');try{const list=await AI.listModels(x);if(!list.length)throw new Error('服務商沒有回傳可用模型');const select=$('#apiModelList');select.innerHTML=`<option value="">選擇模型</option>${list.map(m=>`<option value="${esc(m)}">${esc(m)}</option>`).join('')}`;select.classList.remove('hidden');const saved=$('#apiModelManual').value;if(saved&&list.includes(saved))select.value=saved;setStatus(`✓ 已讀取 ${list.length} 個模型`)}catch(e){setStatus(`讀取失敗：${e?.message||e}`,true)}finally{btn.disabled=false}}
async function testApi(){const x=selected(),error=validate(x);if(error)return setStatus(error,true);const btn=$('#testApi');btn.disabled=true;setStatus('正在測試 API…');try{const t=await AI.call({...x,messages:[{role:'user',content:'只回覆 OK'}],maxTokens:16,temperature:0});setStatus(`✓ 連線成功：${String(t).trim().slice(0,40)||'OK'}`)}catch(e){setStatus(`測試失敗：${e?.message||e}`,true)}finally{btn.disabled=false}}
function open(){const s=current(),a=auto(),boot=window.OCLifeBoot?.enabled?.()??true;modal(`<h2>設定</h2><div style="padding:11px 12px;margin-bottom:15px;border-radius:15px;background:rgba(255,255,255,.42);border:1px solid var(--line);display:flex;align-items:center;justify-content:space-between;gap:12px"><div><b style="font-size:13px">第一次使用？</b><div class="note">重新查看世界、角色與 API 的新手教學</div></div><button type="button" class="secondary" id="openTutorial">新手教學</button></div><h3>介面</h3><div class="field"><label><input id="bootAnimation" type="checkbox" ${boot?'checked':''}> 開啟入場動畫</label><div class="note">啟動時顯示系統初始化代碼與進度條</div></div><button type="button" class="secondary" id="previewBoot">預覽入場動畫</button><hr style="border:0;border-top:1px solid var(--line);margin:18px 0"><h3>AI 與模型</h3><div class="field"><label>API 服務商</label><select id="apiProvider">${Object.entries(PRESETS).map(([id,p])=>`<option value="${id}" ${id===s.provider?'selected':''}>${esc(p.label)}</option>`).join('')}</select><div class="note" id="apiProviderHint"></div></div><div class="field"><label>Base URL</label><input id="apiBase" value="${esc(s.baseUrl)}" placeholder="切換服務商後自動填入，也可以手動修改"></div><div class="field"><label>API Key</label><div style="display:flex;gap:7px"><input id="apiKey" type="password" value="${esc(s.apiKey)}" style="flex:1"><button type="button" class="secondary" id="toggleApiKey">顯示</button></div></div><div class="field"><label>模型</label><input id="apiModelManual" value="${esc(s.model)}" placeholder="輸入模型 ID，或按下方讀取模型"><select id="apiModelList" class="hidden" style="margin-top:7px"><option value="">尚未讀取模型</option></select></div><div class="toolbar" style="display:grid;grid-template-columns:1fr 1fr;gap:8px"><button type="button" class="secondary" id="loadApiModels">讀取模型</button><button type="button" class="secondary" id="testApi">測試連線</button></div><button type="button" class="primary" id="saveApiNow" style="width:100%;margin-top:9px;padding:12px">保存目前 API</button><div id="apiStatus" class="note" style="min-height:18px;margin-top:7px"></div><div style="margin-top:13px;padding:11px;border:1px solid var(--line);border-radius:14px"><b style="font-size:13px">API 設定檔</b><div class="note">可保存多組服務商、Key 和模型，之後一鍵載入。</div><div style="display:flex;gap:7px;margin-top:8px"><input id="apiProfileName" placeholder="設定檔名稱，例如：SiliconFlow 日常" style="flex:1"><button type="button" class="secondary" id="saveApiProfile">另存設定檔</button></div><div id="apiProfiles" style="margin-top:7px"></div></div><hr style="border:0;border-top:1px solid var(--line);margin:18px 0"><h3>自動生活</h3><div class="two"><div class="field"><label>最短分鐘</label><input id="autoMin" type="number" value="${a.minMinutes}"></div><div class="field"><label>最長分鐘</label><input id="autoMax" type="number" value="${a.maxMinutes}"></div></div><div class="field"><label><input id="autoEnabled" type="checkbox" ${a.enabled?'checked':''}> 開啟自動生活</label><label><input id="autoChat" type="checkbox" ${a.chat?'checked':''}> 聊天</label><label><input id="autoMoment" type="checkbox" ${a.moment?'checked':''}> 動態</label><label><input id="autoEvent" type="checkbox" ${a.event?'checked':''}> 事件</label><label><input id="autoStatus" type="checkbox" ${a.status?'checked':''}> 現在狀態</label></div><div class="actions"><button type="button" class="secondary" id="apiCancel">關閉</button><button type="button" class="primary" id="apiSave">儲存全部設定</button></div>`);
$('#openTutorial').onclick=()=>{close();window.OCLifeOnboarding?.open?.()};$('#previewBoot').onclick=()=>window.OCLifeBoot?.run?.(true);$('#apiProvider').onchange=changeProvider;$('#loadApiModels').onclick=loadModels;$('#apiModelList').onchange=e=>{if(e.target.value)$('#apiModelManual').value=e.target.value};$('#toggleApiKey').onclick=()=>{const key=$('#apiKey'),show=key.type==='password';key.type=show?'text':'password';$('#toggleApiKey').textContent=show?'隱藏':'顯示'};$('#testApi').onclick=testApi;$('#saveApiNow').onclick=()=>persist();$('#saveApiProfile').onclick=saveProfile;$('#apiCancel').onclick=close;$('#apiSave').onclick=()=>{if(!persist({quiet:true}))return;window.OCLifeBoot?.setEnabled?.($('#bootAnimation').checked);window.OCLifeAutoLife?.save?.({...a,enabled:$('#autoEnabled').checked,chat:$('#autoChat').checked,moment:$('#autoMoment').checked,event:$('#autoEvent').checked,status:$('#autoStatus').checked,minMinutes:+$('#autoMin').value||8,maxMinutes:+$('#autoMax').value||22});close();window.OCLifeAutoLife?.toast?.('✓ 全部設定已儲存')};updateProviderHint();renderProfiles()}
function install(){if(document.documentElement.dataset.ocProviderSettingsRouter)return;document.documentElement.dataset.ocProviderSettingsRouter='1';document.addEventListener('click',e=>{const b=e.target.closest?.('#settingsBtn');if(!b)return;e.preventDefault();e.stopImmediatePropagation();open()},true);const b=document.getElementById('settingsBtn');if(b)b.onclick=null}
window.OCLifeProviderSettings={version:'2.0.0',open,install,PRESETS,profiles,persist};document.readyState==='loading'?document.addEventListener('DOMContentLoaded',install,{once:true}):install();
})();