(function(){
'use strict';
const S=window.OCLifeStore;
function style(){if(document.getElementById('oc-moment-tools-style'))return;const s=document.createElement('style');s.id='oc-moment-tools-style';s.textContent=`.oc-moment-delete{margin-left:auto;border:0;background:#f4e5e3;color:#a24f4b;border-radius:11px;padding:7px 10px;font-size:12px;font-weight:700}.oc-moment-tools-head{display:flex;justify-content:flex-end;margin:0 0 12px}.oc-clear-moments{border:1px solid #e2c7c3;background:#fff7f5;color:#a24f4b;border-radius:13px;padding:9px 12px;font-weight:700}`;document.head.appendChild(s)}
function worldId(){return window.OCLifeWorldContext?.get?.()||window.OCLifePhone?.activeWorldId||null}
function inject(){style();if(document.querySelector('#pageTitle')?.textContent!=='動態')return;const wid=worldId();if(!wid)return;const view=document.querySelector('#view');if(!view)return;if(!view.querySelector('.oc-moment-tools-head')){const first=view.firstElementChild,head=document.createElement('div');head.className='oc-moment-tools-head';head.innerHTML='<button class="oc-clear-moments">清空本世界動態</button>';first?.after(head);head.querySelector('button').onclick=()=>{const n=S.all('moments',{worldId:wid}).length;if(!n)return alert('目前沒有動態');if(!confirm(`確定刪除這個世界的 ${n} 條動態？此操作不會刪角色或聊天。`))return;S.data.moments=S.data.moments.filter(m=>m.worldId!==wid);S.save();document.querySelector('#worldNav [data-tab="moments"]')?.click()}}
 const moments=S.all('moments',{worldId:wid}).sort((a,b)=>b.at-a.at),cards=[...view.querySelectorAll('.moment-card')];cards.forEach((card,i)=>{if(card.querySelector('.oc-moment-delete')||!moments[i])return;const btn=document.createElement('button');btn.className='oc-moment-delete';btn.textContent='刪除';const top=card.querySelector('.row');top?.appendChild(btn);btn.onclick=()=>{const m=moments[i];if(!confirm('確定刪除這條動態？'))return;S.remove('moments',m.id);document.querySelector('#worldNav [data-tab="moments"]')?.click()}})}
new MutationObserver(()=>requestAnimationFrame(inject)).observe(document.documentElement,{childList:true,subtree:true});inject();window.OCLifeMomentTools={version:'1.0.0'};
})();