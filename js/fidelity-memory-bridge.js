(function(){'use strict';const F=()=>window.OCLifeFidelity;function install(){const f=F();if(!f||f.__memoryBridge)return;f.__memoryBridge=true;const oldEvent=f.eventContext?.bind(f);f.eventContext=function(worldId,ids,limit){const events=oldEvent?oldEvent(worldId,ids,limit):'',mem=f.memoryContext?.(worldId,ids,8)||'';return[events,mem&&`重要提問記憶：\n${mem}`].filter(Boolean).join('\n\n')}}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',install,{once:true}):install();setTimeout(install,0);window.addEventListener('oclife:question-memory-change',install);window.OCLifeFidelityMemoryBridge={version:'1.0.0',install};})();